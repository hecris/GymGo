import {
    StyleSheet,
} from 'react-native';

export default ShopScreenStyles = StyleSheet.create({
    balanceLbl: {
        fontWeight: 'bold',
        fontSize: 24,
        marginTop: 15,
        marginLeft: 10,
    },

    shopItem: {
        backgroundColor: 'whitesmoke',
        marginLeft: 10,
        marginBottom: 50,
    },

    itemName: {
        fontWeight: 'bold',
        fontSize: 18,
        color: 'green',
    },

    itemPrice: {
        fontSize: 16,
        fontWeight: 'bold',
        marginBottom: 5,
    }

}
)