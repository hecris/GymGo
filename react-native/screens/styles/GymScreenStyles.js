import {
  StyleSheet,
} from 'react-native';

export default GymScreenStyles = StyleSheet.create({
  GymName: {
    fontSize: 22,
    fontWeight: 'bold',
    color: 'green',
  },

  GymAddress: {
    fontSize: 18,
  },
  
});